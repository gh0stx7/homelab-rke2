defaultNamespace: cert-manager

# cert-manager configuration
# ClusterIssuers and Certificate resources

kustomize:
  dir: ./

labels:
  role: security
  component: cert-manager-config

dependsOn:
  - name: homelab-rke2-infra-cert-manager