defaultNamespace: metallb-system

# MetalLB - Bare metal LoadBalancer implementation
# Provides LoadBalancer service type for on-premises clusters

helm:
  repo: https://metallb.github.io/metallb
  chart: metallb
  version: 0.14.9
  releaseName: metallb
  waitForJobs: true

labels:
  role: networking
  component: load-balancer