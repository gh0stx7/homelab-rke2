defaultNamespace: unifi

# UniFi Network Application
# Central management for UniFi access points and network devices

labels:
  app: unifi-network
  stack: networking

dependsOn:
  - name: homelab-rke2-infra-cert-manager-config